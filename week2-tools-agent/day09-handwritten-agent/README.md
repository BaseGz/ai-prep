# D9 · 手写 Agent 循环（脱离框架）

> **重点日** ｜ 目录 `week2-tools-agent/day09-handwritten-agent/` ｜ 预计 3h ｜ 对应 2026-09-29（周二） ｜ 阶段：第 2 周 工具与 Agent

## 一、今天学什么

| 类型 | 内容 | 时长 |
|---|---|---|
| 视频 | 尚硅谷 第 05 章 Tools 后半（强制使用工具等） | 约 40 分钟 |
| 文档 | 官方 Tools 页面 → 工具执行与返回值处理 | 20 分钟 |

## 二、今天动手做什么

- [ ] 不用任何框架，用 while 循环手写 Agent：调模型 → 有 tool_calls 就执行 → 结果塞回消息 → 再调模型，直到没有 tool_calls 为止（90 分钟）

## 三、今天提交什么

在 PyCharm 里分 **2 次** 提交。Ctrl + K 打开提交窗口后，**记得取消勾选不属于本次提交的文件**。

### 第 1 条

```text
feat(day09): 手写 while 循环 Agent，脱离框架

现象：模型返回的 tool_calls 结构看不懂，第一次直接把它塞回 messages 报错
原因：tool_calls 是「意图」不是「消息」，必须先执行工具，再把结果包成 ToolMessage 回传
做法：循环 调模型 → 有 tool_calls 就执行 → 追加 ToolMessage → 直到没有 tool_calls 为止
结论：框架替我做的就是循环控制 + 消息组装 + 异常兜底；模型本身没有任何执行权限
```

### 第 2 条

```text
docs(decisions): 手写循环 vs create_agent 的职责边界

模型只输出意图，没有任何执行权限
执行、消息组装、异常兜底，全是我的代码在做
```

## 四、今天必须留下的

- [ ] 代码当天跑通
- [ ] 日志 → `docs/journal/2026-09-29.md`
- [ ] 一句话结论：________________________________
- [ ] **重点日：把今天的对比数据/结论额外整理一份，D35 演示要用**

---

> 这 30 行代码比看 10 小时教程有用。做完你就知道框架到底替你做了什么——这是面试最能体现深度的一题。
