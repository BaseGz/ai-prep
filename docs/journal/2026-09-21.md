# 2026-09-21 · D1 环境搭建 + LangChain 全景

## 一句话结论

（填完再提交。例：环境一次配死 —— 3.12.8 venv + langchain 1.4.2，后面 34 天不再碰配置；LangChain 的本质是 Model + Harness，框架的价值在「换供应商只改两行」）
________________________________

## 今天的坑

现象：跑 `hello_model.py` 时控制台刷 LangSmith 401
（`api.smith.langchain.com/runs/multipart` Unauthorized），但模型回复本身是正常的。
我以为：401 是 DeepSeek 的 key 失效、或者 base_url 配错。
实际：报错来自 **LangSmith 追踪服务**（另一个域名、另一套凭据）。`.env` 里
`LANGSMITH_TRACING=true` 但 `LANGSMITH_API_KEY` 为空 —— 开关开着却没钥匙，就一直被拒。
怎么定位到的：看报错里的**域名** —— 是 `smith.langchain.com` 而不是 `deepseek.com`。
已修：`.env` / `.env.example` 的 `LANGSMITH_TRACING` 改成 `false`，重跑验证输出干净。
（完整四段式已抄进 `errors/index.md`）

## 明天第一件事

**D4：Message 与提示词模板**（D2 / D3 的内容此前已自学过，跳过实操）。
动手：手动拼 messages 数组做 3 轮对话，全程不用任何记忆类 →
`week1-basics/day04-messages/messages_demo.py`

---

## 今天用到的精确坐标（方便以后回看）

- 尚硅谷 BV1rv7A6oEeP：P01–P06 + P10，共 39 分 35 秒
- 全模块拆解 BV1WgcZzRE9E：第 1 讲 课程简介 29:51
- 文档：`docs.langchain.com/oss/python/langchain/overview`、`/oss/python/langchain/install`
- 代码：`env_check.py`（自检四项）、`hello_model.py`（首次调用）
